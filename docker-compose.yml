services:
  frontend:
    build: ./AutoTranscriber
    ports:
      - "5173:5173"
    volumes:
      - ./AutoTranscriber:/app
      - /app/node_modules
    environment:
      - VITE_API_URL=http://localhost:8000